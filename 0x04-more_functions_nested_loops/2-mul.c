#include "main.h"

/**
 * mul - computes the product of two numbers
 *
 *@a: First digit to input
 *@b: Second digit to input
 * Return: returns the results of the multiplication
 */

int mul(int a, int b)
{
	return (a * b);
}
