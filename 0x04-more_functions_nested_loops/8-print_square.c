#include "main.h"
#include <stdlib.h>

/**
 *print_square - it prints squares
 *@size: value to work with
 *Return: Always 0.
 */

void print_square(int size)
{  
	int i; 

	for (i = 0; i < abs(size); i++)
	{
	for (i = 0; i < abs(size); i++)
		_putchar('\#');
	}
    		_putchar('\n');
	}	
	_putchar('\n');
}
